import type { Drop } from "@nebula/shared";
import { drops as originalDrops } from "./drops";

// 🎯 Simplified Intelligent Drops - using existing structure
export const simpleIntelligentDrops: Drop[] = [
  ...originalDrops,
  // Add more drops here if needed
];

