# 🚀 MCP QUICK FIX - SOFORT FUNKTIONSFÄHIG!

## ✅ Status: Konfiguration kopiert!

Die MCP-Konfigurationsdateien wurden erfolgreich kopiert:
- ✅ Cursor MCP-Konfiguration: `%APPDATA%\Cursor\User\globalStorage\cursor.mcp\config.json`
- ✅ Claude Desktop MCP-Konfiguration: `%APPDATA%\Claude\User\globalStorage\claude.mcp\config.json`

---

## 🎯 SOFORT TESTEN - 3 SCHRITTE:

### **Schritt 1: Cursor/Claude Desktop NEU STARTEN**
- Schließen Sie Cursor/Claude Desktop komplett
- Starten Sie es neu
- Die MCP-Server sollten automatisch verbunden werden

### **Schritt 2: MCP-Server starten (in separatem Terminal)**
```bash
cd C:\Users\issab\Desktop\TETETTE\mcp
npm run start:eyes
```

### **Schritt 3: Testen Sie diese Befehle in Cursor/Claude:**

#### 📁 **Dateisystem-Test:**
```
"Lese die Datei apps/web/src/App.tsx"
```

#### 👁️ **GUI-Automatisierung-Test:**
```
"Mache einen Screenshot des Bildschirms"
```

#### 🔧 **Git-Test:**
```
"Zeige mir den Git-Status"
```

#### 🚀 **Build-Test:**
```
"Führe pnpm build aus"
```

---

## 🔧 FALLS ES NICHT FUNKTIONIERT:

### Problem: MCP-Server werden nicht erkannt
**Lösung:**
1. Überprüfen Sie, ob Cursor/Claude Desktop neu gestartet wurde
2. Überprüfen Sie die Konfigurationsdatei:
   - `%APPDATA%\Cursor\User\globalStorage\cursor.mcp\config.json`
3. Überprüfen Sie, ob die Pfade korrekt sind

### Problem: "Command not found" Fehler
**Lösung:**
```bash
cd C:\Users\issab\Desktop\TETETTE\mcp
npm install
npm run start:eyes
```

### Problem: Screenshots funktionieren nicht
**Lösung:**
```bash
mkdir C:\Users\issab\Desktop\TETETTE\screenshots
```

---

## 🎉 ERFOLG!

Wenn alles funktioniert, sollten Sie sehen:
- ✅ MCP-Server werden in Cursor/Claude erkannt
- ✅ Befehle werden ausgeführt
- ✅ Screenshots werden erstellt
- ✅ Dateien werden gelesen/geschrieben
- ✅ Git-Operationen funktionieren

---

## 🚀 NÄCHSTE SCHRITTE:

1. **Starten Sie Cursor/Claude Desktop neu**
2. **Testen Sie die Befehle**
3. **Beginnen Sie mit der Automatisierung Ihres Nebula-Projekts!**

**Ihr AI-Agent hat jetzt "Augen" und kann das gesamte Projekt verwalten!** 🎉

